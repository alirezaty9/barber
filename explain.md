<div dir="rtl" align="right">

![نوع](https://img.shields.io/badge/%D9%86%D9%88%D8%B9-%D8%AA%D9%88%D8%B6%DB%8C%D8%AD_%DA%A9%D8%AF-purple) ![کد](https://img.shields.io/badge/%DA%A9%D8%AF-%D9%86%D9%88%D8%B4%D8%AA%D9%87_%D8%B4%D8%AF-success)

# 📝 توضیح ریز کدها — این نوبت

---

## 🗂️ فهرست
1. [باگ چشمک و علت دقیقش](#a)
2. [حذف بَج هیرو](#b)
3. [اطلاعات تماس](#c)
4. [تصمیم‌ها و مزایا/معایب](#d)

---

<a name="a"></a>
## ✨ ۱) چرا خط زیر عکس «چشمک» می‌زد؟ (ریز)

ساختار کارت این است: یک کادر `relative h-44 overflow-hidden rounded-2xl` و داخلش `<img className="absolute inset-0 object-cover">`.

وقتی روی کارت هاور می‌کردی، کلاس `group-hover:scale-105` عکس را ۵٪ بزرگ می‌کرد. مشکل کلاسیک مرورگرها (به‌ویژه Chrome):

> هنگام **انیمیشنِ transform** روی فرزندی که داخل والدِ `overflow-hidden` با `border-radius` است، مرز برش (clip) با محاسبات زیرپیکسلی همگام نمی‌شود و یک خط نازک ۱px لب پایین، برای چند فریم ظاهر/محو می‌شود → همان «چشمک».

**جایگزین‌هایی که بررسی کردم:**
1. افزودن `transform-gpu`/`backface-hidden` → گاهی کم می‌کند ولی **تضمینی نیست** و در همه مرورگرها یکسان نیست.
2. حذفِ کاملِ افکت هاور → باگ می‌رود ولی کارت بی‌روح می‌شود.
3. **(انتخاب‌شده)** عوض‌کردن `scale` با `brightness` → چون `filter: brightness` **هیچ هندسه‌ای را جابه‌جا نمی‌کند**، مرز برش ثابت می‌ماند و درز به‌کلی حذف می‌شود.

```jsx
// transition-transform + scale  →  درز هنگام انیمیشن
// transition + brightness       →  بدون تغییر لبه، بدون درز
className="absolute inset-0 w-full h-full object-cover transition duration-500 group-hover:brightness-110"
```

نکته‌ی کارایی: `brightness` یک فیلتر GPU سبک است؛ تفاوت محسوسی با `transform` ندارد.

---

<a name="b"></a>
## 🧹 ۲) حذف بَج بالای هیرو

کل بلوک `<div className="inline-flex ...">` که نقطه‌ی `animate-pulse` و متن «رزرو آنلاین با تایید آنی» را داشت حذف شد. کلاس `mb-8` آن هم رفت، پس فاصله‌ی اضافی نمی‌ماند و تیتر `h1` کمی بالاتر می‌نشیند (با `mb-6` خودش).

---

<a name="c"></a>
## 📞 ۳) اطلاعات تماس (دو فایل)

| فایل | فیلد | روش |
|------|------|------|
| `LandingHero.jsx` | آدرس/تلفن/ساعت در نوار پایین | متن مستقیم `<span>` |
| `Footer.jsx` | آدرس/تلفن/ساعت در ستون «ارتباط با ما» | تلفن با `toPersianDigits('۰۹۱۹۵۴۹۶۹۲۹')` |

`toPersianDigits` در فوتر همچنان استفاده می‌شود (هم برای تلفن هم سال)، پس importی بلااستفاده نشد. ساعت در فوتر از حالت `«۱۰:۰۰ الی ۲۱:۰۰»` به متن ساده‌ی `«۹ صبح تا ۱۰ شب»` تغییر کرد.

---

<a name="d"></a>
## 🧠 ۴) تصمیم‌ها — خوبی‌ها/بدی‌ها (شماره‌دار)

۱) **brightness به‌جای scale** — خوبی‌ها: رفع قطعی باگ + حفظ افکت؛ بدی‌ها: نبودِ زوم. 

۲) **هماهنگ‌سازی فوتر با هیرو** — خوبی‌ها: اطلاعات تماس یکدست؛ بدی‌ها: یک ویرایش بیشتر.

۳) **حذف کامل بلوک بَج (نه فقط متن)** — خوبی‌ها: بدون فاصله/المان خالی باقی‌مانده؛ بدی‌ها: ندارد.

۴) **ارقام فارسی تلفن** — خوبی‌ها: یکدستی بصری؛ بدی‌ها: برای لینک `tel:` بعداً نسخه‌ی لاتین لازم می‌شود.

</div>

<div dir="rtl" align="right">

![کد](https://img.shields.io/badge/%DA%A9%D8%AF_%D9%86%D9%88%D8%B4%D8%AA%D9%87_%D8%B4%D8%AF%D8%9F-%D8%AE%DB%8C%D8%B1_%E2%80%94_%D8%B9%DB%8C%D8%A8%E2%80%8C%DB%8C%D8%A7%D8%A8%DB%8C-orange)

# 🔬 توضیح فنی — چرا لوکال به دیتابیس نمی‌رسد

> 📌 در این نوبت کدی نوشته نشد؛ خطای زمان‌اجرا تحلیل شد.

---

## 🧠 ریشه

```
PrismaClientInitializationError: Can't reach database server at ...:6543
```
- این `P1001` (عدم دسترسی شبکه) است، نه خطای کد یا رمز.
- علت همان **DPI/تحریمِ** ایران است که قبلاً تشخیص دادیم: فایروال ترافیک واقعی به پورت دیتابیس Supabase را قطع می‌کند.
- این‌بار پورت `6543` است (Pooled، چون اپ در زمان اجرا از `DATABASE_URL` استفاده می‌کند)، در حالی‌که قبلاً `migrate` از `5432` می‌رفت. هر دو فیلتر می‌شوند.

---

## 🧩 چرا روی Vercel مشکلی نیست؟

| محیط | به دیتابیس می‌رسد؟ |
|------|--------------------|
| لپ‌تاپ تو (ایران، بدون VPN) | ❌ فیلتر |
| سرورهای Vercel (فرانکفورت/آمریکا) | ✅ بله |

اتصال دیتابیس از جایی برقرار می‌شود که کد **اجرا** می‌شود. در `npm run dev`، کد روی لپ‌تاپ توست؛ روی پروداکشن، کد روی Vercel است.

---

## 🧩 چرا `GET / 200` با وجود خطا؟

`app/page.jsx` یک Server Component است که در `error boundary` (فایل `app/error.jsx`) محصور است. وقتی `prisma.findMany` throw کرد، Next آن را گرفت و صفحه‌ی خطا را با وضعیت ۲۰۰ نشان داد (به‌جای کرش). این رفتار درست و امن است.

---

## 🧩 چرا انتقال به فرانکفورت این را حل نمی‌کند؟

تحریم Supabase بر اساس **آی‌پی مبدأ (ایران)** است، نه ناحیه‌ی دیتابیس. پس دیتابیسِ فرانکفورت هم از لپ‌تاپ ایرانیِ بدون VPN در دسترس نخواهد بود. انتقال فرانکفورت برای **سرعتِ کاربرانِ نهاییِ سایت** و **اتصال از Vercel** است، نه برای dev لوکال.

---

## 📝 تصمیم‌ها و مزایا/معایب (شماره‌دار)

۱) **اعلام «باگ نیست، فیلترینگ است»** — خوبی: جلوگیری از تغییر بی‌مورد کد. بدی: ندارد. چرا: خطای `P1001` صریحاً شبکه‌ای است و قبلاً هم همین را دیدیم.

۲) **پیشنهاد تست روی Vercel به‌جای لوکال** — خوبی: بدون VPN، محیط واقعی. بدی: چرخه‌ی توسعه کندتر. چرا: تنها راه لوکال، VPN است.

۳) **عدم بازگرداندن SQLite برای dev لوکال** — خوبی: حفظ سادگی و یکدستی postgres. بدی: dev لوکال نیازمند VPN می‌ماند. چرا: سوییچ provider دردسر و ریسک قبلی را برمی‌گرداند؛ ارزشش را ندارد.

</div>

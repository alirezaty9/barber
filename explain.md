<div dir="rtl" align="right">

![نوع](https://img.shields.io/badge/%D8%A7%DB%8C%D9%86%20%D9%86%D9%88%D8%A8%D8%AA-%D8%B1%D8%A7%D9%87%E2%80%8C%D8%A7%D9%86%D8%AF%D8%A7%D8%B2%DB%8C%20%D8%AF%D9%88%D9%85%D8%AD%DB%8C%D8%B7%DB%8C-blue)

# 🔬 توضیح فنی — چرا لوکال و آنلاین با یک کد کار می‌کنند؟

## 📂 در این نوبت چه شد؟
کد اپ تغییر نکرد. فقط وضعیت تأیید شد (پورت 5432 + وجود `barber`) و **مسیر کامل کردن هر دو محیط** مستند شد.

---

## 🧩 مدل ذهنی: یک کد، دو دیتابیس

```
                 Prisma  →  env("DATABASE_URL")
                              │
        ┌─────────────────────┴─────────────────────┐
   💻 لوکال                                      ☁️ Vercel
   منبع env: فایل .env                           منبع env: داشبورد Vercel
   مقدار: localhost:5432/barber                  مقدار: Supabase pooler:6543
```
کد یکی است؛ فقط **منبع و مقدارِ `DATABASE_URL`** در دو محیط فرق می‌کند. برای همین بدون هیچ شاخه‌بندی در کد، هر دو کار می‌کنند.

---

## 🧩 چرا هنوز «آماده‌ی کار» نیست هرچند کانفیگ درست است؟

کانفیگ فقط می‌گوید «به کدام دیتابیس وصل شو». اما:
1. دیتابیس باید **جدول** داشته باشد → `prisma db push` (schema را می‌سازد).
2. اگر داده‌ی اولیه می‌خواهی → `db:seed`.

این‌ها **عملیات روی خود دیتابیس‌اند**، نه فایل. لوکال و Supabase دو دیتابیس مجزا هستند، پس این دو مرحله باید **روی هرکدام جداگانه** اجرا شوند.

---

## 🧩 ترفند override با متغیر جلوی دستور

```bash
DATABASE_URL="...supabase..." npx prisma db push
```
- در Prisma/dotenv، **متغیرهای محیطیِ از قبل ست‌شده، بر مقدار `.env` اولویت دارند** (dotenv مقادیر موجود را بازنویسی نمی‌کند).
- پس این دستور فقط برای همان یک اجرا به Supabase می‌خورد و بعدش `.env` دوباره لوکال است. **بدون ویرایش فایل، بدون ریسکِ فراموشی.**

مثال ملموس: مثل این است که فقط برای یک تماس، سیم‌کارت دوم را بگذاری؛ بعد از تماس خودکار برمی‌گردی به سیم‌کارت اول.

---

## 🧩 چرا روی Vercel هم `DIRECT_URL` لازم است؟

| متغیر | نقش | چرا |
|---|---|---|
| `DATABASE_URL` (6543, pgbouncer) | اتصال زمان اجرا | Serverless روی Vercel هر درخواست کانکشن جدید می‌زند؛ pooler ازدحام کانکشن را مدیریت می‌کند |
| `DIRECT_URL` (5432) | migrate / db push | pgbouncer برای عملیات schema مناسب نیست؛ Prisma برای این‌ها اتصال مستقیم می‌خواهد |

بدون `DIRECT_URL`، مهاجرت‌ها یا `db push` روی محیط پولد می‌شکنند.

---

## 🧩 بیلد Vercel چطور Prisma را آماده می‌کند؟
در `package.json`:
```json
"postinstall": "prisma generate"
```
هنگام `npm install` روی Vercel، کلاینت Prisma دوباره تولید می‌شود (وگرنه به‌خاطر کش، کلاینت قدیمی می‌ماند و ارور می‌دهد). پس نیازی به کار دستی نیست.

---

## 📝 تصمیم‌ها و مزایا/معایب (شماره‌دار)

۱) **override با متغیر جلوی دستور به‌جای ویرایش `.env`** — خوبی‌ها: بدون ریسک فراموشی، `.env` لوکال امن. بدی‌ها: دستور طولانی. چرا: جلوگیری از اجرای اشتباهی روی محیط اشتباه.

۲) **جداکردن مرحله‌ی «کانفیگ» از «داده»** — خوبی‌ها: شفافیت؛ کاربر می‌فهمد چرا هنوز کار می‌ماند. بدی‌ها: مراحل بیشتر. چرا: صداقت فنی؛ کانفیگ درست ≠ دیتابیس آماده.

۳) **ست هر دو URL روی Vercel** — خوبی‌ها: هم runtime هم migrate کار می‌کنند. بدی‌ها: دو مقدار. چرا: الگوی رسمی Supabase+Prisma.

---

## ⚠️ تله‌ها
- اگر روی Vercel فقط `DATABASE_URL` بگذاری و `DIRECT_URL` نه → ممکن است بیلد/مهاجرت خطا دهد.
- اگر `db push` روی Supabase را نزنی، سایت آنلاین وصل می‌شود ولی جدول ندارد → ارور «relation does not exist».
- seed را دوبار روی یک دیتابیس نزن مگر اسکریپت idempotent باشد (وگرنه داده تکراری).

</div>

<div dir="rtl" align="right">

![نوع](https://img.shields.io/badge/%D9%86%D9%88%D8%B9-%D8%AA%D9%88%D8%B6%DB%8C%D8%AD_%DA%A9%D8%AF-purple) ![کد](https://img.shields.io/badge/%DA%A9%D8%AF-%D9%86%D9%88%D8%B4%D8%AA%D9%87_%D8%B4%D8%AF-success)

# 📝 توضیح ریز کدها — این نوبت

---

## 🗂️ فهرست
1. [نمایش دو خدمت در پنل](#a)
2. [دسته‌بندی جدید «گریم داماد»](#b)
3. [تصمیم‌ها و مزایا/معایب](#c)

---

<a name="a"></a>
## 🛠️ ۱) نمایش هر دو خدمت در پنل

**API (`app/api/bookings/route.js` — متد GET):** به `include` کوئریِ فهرست، `service2: true` اضافه شد تا داده‌ی خدمت دوم هم برگردد.

**کامپوننت (`BookingsManager.jsx`):**
```jsx
// نام هر دو خدمت با + کنار هم
{[b.service, b.service2].filter(Boolean).map((s) => s.name).join(' + ') || 'خدمت نامشخص'}

// جمع قیمت هر دو خدمت
{(b.service || b.service2) ? formatPrice((b.service?.price || 0) + (b.service2?.price || 0)) : ''}
```
- `filter(Boolean)` خدمت‌های null را حذف می‌کند (اگر فقط یک خدمت باشد، فقط همان نشان داده می‌شود).
- `?.` و `|| 0` از خطا در نبودِ خدمت دوم جلوگیری می‌کند.

---

<a name="b"></a>
## 🤵 ۲) دسته‌بندی جدید «گریم داماد» (`groom`)

عکسِ کارت‌های خدمات بر اساس **دسته‌بندی** انتخاب می‌شود (نه تک‌تک خدمت). پس برای «عکس مخصوص داماد»، تمیزترین راه افزودن یک دسته‌ی جدید بود. این دسته در چند نقطه باید هماهنگ می‌شد:

| فایل | نقش |
|------|-----|
| `constants.js` → `CATEGORY_LABELS.groom` | برچسب فارسی «گریم داماد» (در پنل و لیست خدمات استفاده می‌شود) |
| `validation.js` → `CATEGORIES` | افزودن `'groom'` به enum مجاز؛ چون فرمِ ادمین از همین آرایه ساخته می‌شود، گزینه‌ی «گریم داماد» خودکار در dropdown ظاهر می‌شود |
| `ServicesSection.jsx` → `CATEGORIES` و `CATEGORY_IMAGES` | تب فیلتر جدید + نگاشت `groom → /images/service-groom.jpg` |
| `seed.js` → آرایه‌ی `SERVICES` | خدمت `s6` با `category: 'groom'` |
| `public/images/service-groom.jpg` | فایل عکس داماد (با چند نامزدِ Unsplash و fallback دانلود شد؛ نامزد اول جواب داد) |

**نکته:** عکس از طریق همان کامپوننت `ServiceCardImage` با `onError` نمایش داده می‌شود؛ اگر فایل نبود، طرح جایگزین می‌آید (کارت هیچ‌وقت خراب نمی‌شود).

---

<a name="c"></a>
## 🧠 ۳) تصمیم‌ها — خوبی‌ها/بدی‌ها (شماره‌دار)

۱) **«گریم داماد» = دسته‌بندی جدید** — خوبی‌ها: عکس اختصاصی، تب فیلتر مجزا، قابل‌استفاده در ادمین؛ بدی‌ها: هماهنگی در چند فایل لازم بود (انجام شد).

۲) **جمع قیمت دو خدمت در پنل** — خوبی‌ها: مبلغ کل واقعی نوبت؛ بدی‌ها: ندارد.

۳) **استفاده از قاب ۳:۲ و کامپوننت موجود برای عکس داماد** — خوبی‌ها: بدون برش و هماهنگ، با fallback امن؛ بدی‌ها: ندارد.

۴) **مقادیر پیش‌فرض خدمت داماد (۸۵۰هزار/۱۲۰دقیقه)** — خوبی‌ها: منطقی برای پکیج داماد؛ بدی‌ها: حدسی است — از پنل ادمین به‌راحتی قابل ویرایش است.

</div>

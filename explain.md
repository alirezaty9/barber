<div dir="rtl" align="right">

![کد](https://img.shields.io/badge/%DA%A9%D8%AF_%D9%86%D9%88%D8%B4%D8%AA%D9%87_%D8%B4%D8%AF%D8%9F-%D8%A8%D9%84%D9%87_%E2%80%94_schema.prisma-brightgreen)

# 🔬 توضیح فنی — اصلاح datasource و نکته‌ی واگرایی نسخه‌ها

> 📌 محتوای درستِ `schema.prisma` برای کپی‌کردن داده شد. اینجا ریز توضیح می‌دهم.

---

## 🧩 تنها تغییر واقعی: بلوک datasource

```diff
 datasource db {
-  provider = "sqlite"
+  provider  = "postgresql"
   url      = env("DATABASE_URL")
+  directUrl = env("DIRECT_URL")
 }
```
- `provider`: نوع دیتابیس را به Postgres تغییر می‌دهد؛ همین خط بود که خطای `the URL must start with file:` را می‌داد.
- `directUrl`: برای migrate/ابزارها؛ روی Vercel هم بی‌ضرر است و الگوی استاندارد Supabase.
- مدل‌ها (`Service`, `Barber`, ...) **اصلاً تغییر نکردند**، چون مشکلی نداشتند.

---

## ⚠️ نکته‌ی مهم: نسخه‌ی گیت‌هاب با محیط من فرق داشت

فایلی که از گیت‌هاب فرستادی، نسخه‌ی **«تک‌خدمته»** است: هر `Booking` فقط یک `serviceId` دارد.

```prisma
serviceId String?
service   Service? @relation(...)
// خبری از serviceId2 نیست
```

اما `setup.sql` که قبلاً ساختم بر اساس نسخه‌ی **«دوخدمته»** (دارای `serviceId2`) محیط من بود. یعنی جدول `Booking` در دیتابیس یک ستون اضافه‌ی `serviceId2` دارد.

### آیا این مشکل‌ساز است؟ خیر ✅
- Prisma هنگام کوئری، ستون‌ها را **صریح** نام می‌برد (`SELECT "id","code",...`).
- یک ستون اضافه‌ی nullable که کلاینت از آن خبر ندارد، فقط **نادیده گرفته می‌شود**.
- نه `SELECT` می‌شکند، نه `INSERT` (چون ستون nullable است و در insert ذکر نمی‌شود).

پس کد تک‌خدمتهٔ تو با جدولِ دارای ستون اضافه، بدون مشکل کار می‌کند.

> 💡 ریشه‌ی این واگرایی: محیط کاری من (`/app/barber`) و ریپوی لپ‌تاپ/گیت‌هاب تو دو نسخه‌ی متفاوت از پروژه بودند. چون Vercel فقط از گیت‌هاب بیلد می‌کند، **منبع حقیقت = گیت‌هاب توست**؛ برای همین فایل را مطابق همان (تک‌خدمته) دادم تا چیزی به‌هم نریزد.

---

## 🔄 چرخه‌ی درست‌شدن بعد از push

```
push schema (postgresql) → Vercel auto-deploy → postinstall: prisma generate
   → کلاینت postgresql ساخته می‌شود → اتصال با DATABASE_URL موفق
   → page.jsx و /api/services داده برمی‌گردانند ✅
```

---

## 📝 تصمیم‌ها و مزایا/معایب (شماره‌دار)

۱) **تغییر فقط datasource، نه مدل‌ها** — **خوبی‌ها:** ریسک صفر، هماهنگ با کد گیت‌هاب. — **بدی‌ها:** ندارد. — **چرا:** فقط `provider` خطا می‌داد.

۲) **پذیرفتن ستون اضافه‌ی `serviceId2` به‌جای drop آن** — **خوبی‌ها:** نیازی به تغییر دوباره‌ی دیتابیس نیست؛ بی‌ضرر. — **بدی‌ها:** یک ستون بلااستفاده در جدول می‌ماند. — **چرا:** پاک‌کردنش ارزش ریسک و مرحله‌ی اضافه را ندارد.

۳) **ارائه‌ی فایل مطابق نسخه‌ی گیت‌هاب (تک‌خدمته)** — **خوبی‌ها:** جلوگیری از واگرایی و باگ‌های ناخواسته. — **بدی‌ها:** فعلاً قابلیت دوخدمته فعال نمی‌شود. — **چرا:** منبع حقیقتِ دیپلوی، گیت‌هاب توست.

</div>

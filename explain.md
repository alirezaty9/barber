<div dir="rtl" align="right">

![کد](https://img.shields.io/badge/%DA%A9%D8%AF_%D9%86%D9%88%D8%B4%D8%AA%D9%87_%D8%B4%D8%AF%D8%9F-%D8%AE%DB%8C%D8%B1_%E2%80%94_%D8%B9%DB%8C%D8%A8%E2%80%8C%DB%8C%D8%A7%D8%A8%DB%8C-orange)

# 🔬 توضیح فنی — تحلیل خطای P1001 در Prisma/Supabase

> 📌 در این نوبت کدی نوشته نشد؛ کار، **عیب‌یابی اتصال دیتابیس** بود.

---

## 🧠 کدهای خطای Prisma — فرق P1001 و P1000

| کد | معنی | علت |
|----|------|-----|
| `P1001` | Can't reach database server | **شبکه**: پورت بسته، فایروال، تحریم، سرور خاموش |
| `P1000` | Authentication failed | **رمز/یوزر اشتباه** |
| `P1003` | Database does not exist | نام دیتابیس غلط |

چون خطای کاربر `P1001` بود → **قطعاً مشکل از رمز نیست.** رشته‌ی اتصال از نظر فرمت درست است؛ فقط بسته‌های TCP به مقصد نمی‌رسند.

---

## 🔍 چه چیزی را تست کردم؟

از محیط خودم `getent hosts` زدم روی هاست pooler:
```
13.114.6.6      ...pooler.supabase.com
57.182.231.186  ...pooler.supabase.com
```
- **DNS درست resolve شد** → یعنی پروژه‌ی Supabase وجود دارد و pause نیست.
- تست TCP از سندباکس من به ۵۴۳۲ و ۶۵۴۳ نرسید، ولی چون محیط من خروجی محدود دارد، این **برای کاربر قطعی نیست** — برای همین دستور `nc -vz` را به کاربر دادم تا از شبکه‌ی خودش تست کند.

---

## 🧪 چرا `nc -vz host port`؟

`nc` (netcat) با فلگ `-v` (verbose) و `-z` (فقط اسکن، بدون ارسال داده) یک **TCP handshake خام** می‌زند:
- `succeeded` → پورت از سمت کاربر باز و قابل‌دسترس است → مشکل جای دیگر.
- `timed out` → بسته‌ها به مقصد نمی‌رسند → فیلتر/تحریم/فایروال.
- `refused` → به سرور رسید ولی پورت رد کرد (نادر برای این مورد).

این تست **Prisma و رمز را از معادله حذف می‌کند** و فقط لایه‌ی شبکه را می‌سنجد — بهترین راه برای ایزوله‌کردن علت.

---

## 💡 چرا «راه ب» (SQL در داشبورد) کار می‌کند ولی migrate نه؟

- `prisma migrate` به **پورت TCP دیتابیس (۵۴۳۲)** نیاز دارد — همان پورتی که بسته است.
- داشبورد Supabase و SQL Editor روی **HTTPS (پورت ۴۴۳)** کار می‌کنند که تقریباً هیچ‌جا بسته نیست.
- پس اگر SQL ساخت جدول‌ها را آفلاین تولید کنیم (با `prisma migrate diff` که نیازی به اتصال ندارد) و در SQL Editor اجرا کنیم، **کلاً پورت ۵۴۳۲ را دور می‌زنیم.**

---

## 📝 تصمیم‌ها و مزایا/معایب (شماره‌دار)

۱) **تشخیص P1001 = شبکه (نه رمز)** — **خوبی‌ها:** جلوگیری از اتلاف وقت روی رمز. — **بدی‌ها:** ندارد. — **چرا:** کد خطا صریح است.

۲) **دادن تست `nc` به‌جای حدس زدن** — **خوبی‌ها:** علت را دقیق مشخص می‌کند (تحریم vs پورت). — **بدی‌ها:** یک قدم اضافه برای کاربر. — **چرا:** بدون دانستن علت، راه‌حل تیراندازی در تاریکی است.

۳) **آماده‌سازی مسیر SQL آفلاین به‌عنوان پشتیبان** — **خوبی‌ها:** مستقل از فیلترینگ، قطعی. — **بدی‌ها:** seed باید جدا مدیریت شود. — **چرا:** برای شبکه‌های سخت‌گیر تنها راه مطمئن است.

</div>

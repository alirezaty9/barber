// ─────────────────────────────────────────────────────────────
//  کلیدهای روشن/خاموشِ قابلیت‌ها (feature flags) — تنها منبعِ حقیقت.
//
//  🎯 چرا این فایل هست؟ تا «تعلیقِ موقتِ یک قابلیت» با پاک‌کردنِ کد انجام نشود.
//     کدِ پیامک دست‌نخورده سرِ جایش می‌ماند و فقط با یک متغیرِ محیطی خاموش/روشن می‌شود.
//
//  📱 SMS_ENABLED — همه‌ی کارهای وابسته به پنلِ پیامک:
//        • کدِ تأییدِ لغو نوبت (OTP) و در نتیجه «لغو نوبت توسط مشتری»
//        • پیامکِ یادآوریِ نوبت (کرونِ /api/cron/send-reminders)
//
//     خاموش (پیش‌فرض) ⇒ مسیرهای بالا با پیامِ روشن «موقتاً غیرفعال» پاسخ می‌دهند و
//     دکمه‌ی لغو در صفحه‌ی رهگیری نمایش داده نمی‌شود. رزرو و پرداخت هیچ ربطی به این
//     کلید ندارند و همیشه کار می‌کنند.
//
//     روشن‌کردن بعد از خریدِ پنلِ پیامک (بدونِ هیچ تغییری در کد):
//        ۱) در .env (و روی پنلِ هاست، در بخشِ متغیرهای محیطی) بگذار:
//              NEXT_PUBLIC_SMS_ENABLED="true"
//        ۲) SMS_PROVIDER و SMS_API_KEY (و در صورت نیاز SMS_SENDER / SMS_OTP_TEMPLATE)
//           را با اطلاعاتِ پنلِ واقعی پر کن.
//        ۳) پروژه را دوباره بیلد/دیپلوی کن (این متغیر موقعِ بیلد داخلِ کدِ مرورگر جا می‌افتد).
//
//  ⚠️ چرا پیشوندِ NEXT_PUBLIC؟ چون این کلید هم در سرور (مسیرهای API) و هم در مرورگر
//     (نمایش/عدم‌نمایشِ دکمه‌ی لغو) لازم است. متغیرهای بدونِ این پیشوند فقط در سرور
//     در دسترس‌اند و آن‌وقت مجبور می‌شدیم دو متغیرِ جدا نگه داریم.
// ─────────────────────────────────────────────────────────────

export const SMS_ENABLED = process.env.NEXT_PUBLIC_SMS_ENABLED === 'true';

//  💸 REFUNDS_ENABLED — استردادِ وجه هنگامِ لغو نوبت.
//
//     خاموش (پیش‌فرض) ⇒ لغوِ نوبت فقط «لغو» می‌کند: هیچ درخواستِ استردادی به زرین‌پال
//     نمی‌رود و هیچ نوبتی برچسبِ دروغینِ «مسترد شده» نمی‌گیرد. وضعیتِ پرداخت همان
//     «پرداخت‌شده» می‌ماند، یعنی پنل راست می‌گوید: پول گرفته شده و برنگشته.
//
//     چرا خاموش؟ استردادِ واقعیِ زرین‌پال به ZARINPAL_ACCESS_TOKEN نیاز دارد. بدونِ آن،
//     کد استرداد را «فقط دفتری» ثبت می‌کرد و پنل «مسترد شده» نشان می‌داد در حالی که
//     هیچ پولی جابه‌جا نشده بود — یعنی سیستم به صاحبِ کسب‌وکار دروغ می‌گفت.
//
//     روشن‌کردن در آینده: NEXT_PUBLIC_REFUNDS_ENABLED="true" + پرکردنِ ZARINPAL_ACCESS_TOKEN.
//     قواعدِ درصدِ استرداد (مشتری ۵۰٪ / مدیریت ۱۰۰٪) دست‌نخورده در buildCancelPatch مانده‌اند.
export const REFUNDS_ENABLED = process.env.NEXT_PUBLIC_REFUNDS_ENABLED === 'true';

// پیامِ یکسانِ «این قابلیت موقتاً تعلیق است» تا در چند مسیر تکرار نشود.
export const SMS_DISABLED_MESSAGE =
  'لغو آنلاین نوبت موقتاً غیرفعال است. لطفاً برای لغو یا جابه‌جایی نوبت با آرایشگاه تماس بگیرید.';

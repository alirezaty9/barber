<div dir="rtl" align="right">

![وضعیت](https://img.shields.io/badge/%D9%88%D8%B6%D8%B9%DB%8C%D8%AA-%D8%A7%D9%86%D8%AC%D8%A7%D9%85_%D8%B4%D8%AF-brightgreen) ![تغییرها](https://img.shields.io/badge/%D8%AA%D8%BA%DB%8C%DB%8C%D8%B1%D9%87%D8%A7-%DB%B2_%D9%85%D9%88%D8%B1%D8%AF-blue) ![Migrate+Seed](https://img.shields.io/badge/Migrate%2BSeed-%D9%84%D8%A7%D8%B2%D9%85-red)

# 🎯 خلاصه‌ی این نوبت

دو کار انجام شد: **نمایش هر دو خدمت در پنل مدیریت** و **افزودن خدمت «گریم داماد» با عکس مخصوص داماد**.

> 🔴 **حتماً اجرا کن** (تا ستون خدمت دوم ساخته شود و خدمت جدید/عکس‌ها وارد دیتابیس شوند):
> ```bash
> npm run db:migrate
> npm run db:seed
> ```

---

## 🗂️ فهرست مطالب
1. [نمایش هر دو خدمت در پنل](#admin)
2. [افزودن «گریم داماد» + عکس داماد](#groom)
3. [کار لازم تو](#todo)
4. [تصمیم‌ها](#decisions)

---

<a name="admin"></a>
## 🛠️ ۱) نمایش هر دو خدمت در پنل مدیریت

اگر مشتری یک یا **دو خدمت** انتخاب کرده باشد، در فهرست نوبت‌های پنل، **هر دو خدمت** کنار هم نشان داده می‌شوند و **جمع مبلغ** هم محاسبه می‌شود.

| فایل | تغییر |
|------|-------|
| `app/api/bookings/route.js` | به کوئریِ فهرست ادمین `service2` اضافه شد |
| `src/features/admin/BookingsManager.jsx` | نام هر دو خدمت با ` + ` و جمع قیمتِ دو خدمت نمایش داده می‌شود |

مثال نمایش: «اصلاح مو مدرن + گریم داماد» و زیرش جمع مبلغ هر دو.

---

<a name="groom"></a>
## 🤵 ۲) افزودن خدمت «گریم داماد» + عکس داماد

یک **دسته‌بندی جدید به اسم «گریم داماد»** (`groom`) ساخته شد تا هم در منوی خدمات یک تب جدا داشته باشد و هم عکس مخصوص خودش (عکس داماد) را بگیرد.

| فایل | تغییر |
|------|-------|
| `src/lib/constants.js` | برچسب `groom: 'گریم داماد'` |
| `src/lib/validation.js` | `groom` به فهرست دسته‌بندی‌های مجاز اضافه شد (در فرم ادمین هم خودکار می‌آید) |
| `src/features/landing/ServicesSection.jsx` | تب فیلتر «گریم داماد» + نگاشت عکس `groom → /images/service-groom.jpg` |
| `prisma/seed.js` | خدمت `گریم داماد` (۸۵۰٬۰۰۰ تومان، ۱۲۰ دقیقه) افزوده شد |
| `public/images/service-groom.jpg` | ✅ عکس داماد دانلود شد (لوکال، ۴۱KB) |

> 💡 چون دسته‌بندی به‌صورت یک «category» جدید اضافه شد، از این به بعد در **پنل ادمین** هم می‌توانی خدمات دیگری با دسته‌ی «گریم داماد» بسازی و همه‌شان همین عکس داماد را می‌گیرند.

> ✅ عکس داماد با موفقیت در `public/images/service-groom.jpg` ذخیره شد و کاملاً لوکال است.

---

<a name="todo"></a>
## 🔴 ۳) کار لازم تو

```bash
npm run db:migrate   # ساخت ستون خدمت دوم (serviceId2) — از نوبت قبل
npm run db:seed      # ورود خدمت «گریم داماد» + مسیر لوکال عکس‌ها به دیتابیس
npm run dev          # اجرا و تست
```

> 📌 خدمت «گریم داماد» فقط بعد از `db:seed` در سایت ظاهر می‌شود (چون از دیتابیس خوانده می‌شود).

---

<a name="decisions"></a>
## 🧠 ۴) تصمیم‌ها (مزایا/معایب)

**۱) «گریم داماد» به‌صورت دسته‌بندی جدید (نه فقط یک خدمت در دسته‌ی موجود).**
خوبی‌ها: عکس اختصاصیِ داماد می‌گیرد، تب فیلتر جدا دارد، و خدمات بعدیِ مشابه هم خودکار همین عکس را می‌گیرند. بدی‌ها: یک دسته به سیستم اضافه شد (باید در چند فایل هماهنگ می‌شد که شد). چرا: تمیزترین راه برای «عکس مخصوص داماد»، چون عکسِ کارت بر اساس دسته است نه تک‌تک خدمت.

**۲) جمع قیمت دو خدمت در پنل.**
خوبی‌ها: ادمین مبلغ کل نوبت را می‌بیند. بدی‌ها: ندارد. چرا: وقتی دو خدمت نمایش داده می‌شود، مبلغ کل منطقی‌تر از فقط خدمت اول است.

**۳) استفاده از همان قاب ۳:۲ برای عکس داماد.**
خوبی‌ها: هماهنگ با بقیه‌ی کارت‌ها، بدون برش. بدی‌ها: ندارد.

</div>

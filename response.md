<div dir="rtl" align="right">

![وضعیت](https://img.shields.io/badge/%D9%88%D8%B6%D8%B9%DB%8C%D8%AA-%D8%AA%D9%88%D8%B6%DB%8C%D8%AD_%D8%B3%D8%A7%D8%AF%D9%87-brightgreen) ![موضوع](https://img.shields.io/badge/%D9%85%D9%88%D8%B6%D9%88%D8%B9-%D8%AF%DB%8C%D8%AA%D8%A7%D8%A8%DB%8C%D8%B3_%D9%88_%D8%AF%DB%8C%D9%BE%D9%84%D9%88%DB%8C-blue) ![سطح](https://img.shields.io/badge/%D8%B3%D8%B7%D8%AD-%D9%85%D8%A8%D8%AA%D8%AF%DB%8C-orange)

# 🎯 توضیح ساده و کامل (مخصوص شروع کار)

باشه، از اول و **خیلی ساده** توضیح می‌دهم. عجله نکن، قدم‌به‌قدم می‌رویم.

---

## 🗂️ فهرست مطالب
1. [الان چه دیتابیسی داری؟](#db)
2. [اون دستورها یعنی چی؟ (خط به خط)](#cmd)
3. [گذاشتن روی سرور (دیپلوی)](#deploy)
4. [چک‌لیست نهایی](#checklist)

---

<a name="db"></a>
## 🗄️ ۱) الان چه دیتابیسی داری؟

دیتابیس فعلی: **SQLite** (با کمک ابزاری به اسم **Prisma**).

بذار با یک مثال بگم:

| اصطلاح | یعنی چی؟ (به زبان آدمیزاد) |
|---|---|
| 🗄️ **دیتابیس** | یک «انبار» که اطلاعات سایت (نوبت‌ها، آرایشگرها، خدمات) را نگه می‌دارد. |
| 📄 **SQLite** | ساده‌ترین نوع دیتابیس؛ کل انبار فقط **یک فایل** است به اسم `dev.db`. نه نصب سختی دارد، نه سرور جدا. |
| 🔌 **Prisma** | «مترجم» بین کد تو و دیتابیس. تو به فارسیِ کد می‌گویی «نوبت‌ها را بده»، Prisma آن را به زبان دیتابیس ترجمه می‌کند. |

> 💡 **خلاصه:** الان دیتابیست یک فایل ساده روی کامپیوتر خودت است (`prisma/dev.db`). برای **تست و کار روی لپ‌تاپ خودت** عالی است.

> ⚠️ **اما یک نکته‌ی مهم:** این فایل فقط روی کامپیوتر خودت است. وقتی بخواهی سایت را روی **سرور واقعی (اینترنت)** بگذاری، بعضی سرورها اجازه‌ی فایل SQLite نمی‌دهند و باید به یک دیتابیس حرفه‌ای‌تر به اسم **PostgreSQL** سوییچ کنی. (در بخش ۳ کامل می‌گویم.)

---

<a name="cmd"></a>
## ⌨️ ۲) اون دستورها یعنی چی؟ (خط به خط)

اینها دستورهایی هستند که در **ترمینال** (همان پنجره‌ی سیاهِ دستور) داخل پوشه‌ی پروژه می‌زنی. هرکدام یک کار مشخص دارد:

### 📦 دستور اول
```bash
npm install
```
- **یعنی:** همه‌ی ابزارها و کتابخانه‌هایی که پروژه لازم دارد را از اینترنت دانلود و نصب کن.
- **کجا نصب می‌شوند؟** داخل پوشه‌ای به اسم `node_modules` (خودش ساخته می‌شود).
- **کِی بزنم؟** فقط **یک‌بار** اول کار (و هر وقت کتابخانه‌ی جدیدی اضافه شد).

---

### 🔐 دستور دوم
```bash
cp .env.example .env
```
- **یعنی:** از روی فایلِ نمونه (`.env.example`) یک کپی بساز به اسم `.env`.
- **`.env` چیست؟** فایل **تنظیمات محرمانه**؛ رمزها و کلیدها اینجا نوشته می‌شوند (و هیچ‌وقت روی گیت‌هاب نمی‌رود).
- **بعد از کپی، باید این فایل را باز کنی و دو چیز را پر کنی:**

| متغیر | یعنی چی؟ | چی بذارم؟ |
|---|---|---|
| `ADMIN_PASSWORD` | رمز ورود به **پنل مدیریت** (`/admin/login`) | هر رمزی که دوست داری، مثلاً `Royal@1404` |
| `SESSION_SECRET` | یک رشته‌ی **تصادفی و بلند** برای امنیتِ ورود | یک متن طولانی و درهم، مثلاً خروجی دستور پایین |

برای ساختن یک `SESSION_SECRET` امن، این را در ترمینال بزن و خروجی‌اش را کپی کن:
```bash
openssl rand -base64 32
```

> ⚠️ **هشدار:** الان داخل `.env.example` رمز `admin` گذاشته شده که فقط برای تست خوب است. برای سایت واقعی حتماً یک رمز قوی بگذار.

---

### 🏗️ دستور سوم
```bash
npm run db:migrate
```
- **یعنی:** جدول‌های خالی دیتابیس را بساز.
- **مثال ساده:** مثل این است که قفسه‌های خالی انبار را می‌سازی (قفسه‌ی «نوبت‌ها»، قفسه‌ی «آرایشگرها»، …). هنوز چیزی داخلشان نیست.
- این دستور فایل `dev.db` را هم می‌سازد.

---

### 🌱 دستور چهارم
```bash
npm run db:seed
```
- **یعنی:** دیتابیس را با **داده‌های اولیه‌ی نمونه** پر کن.
- **چی اضافه می‌شود؟** ۳ آرایشگر، ۵ خدمت، چند نظر و چند نوبت نمونه — تا سایت خالی و بی‌روح نباشد.
- (`seed` در انگلیسی یعنی «بذر/دانه» → یعنی کاشتنِ داده‌های اولیه.)

---

### ▶️ دستور پنجم
```bash
npm run dev
```
- **یعنی:** سایت را روی **کامپیوتر خودت** روشن کن تا تستش کنی.
- بعد از زدن این، در مرورگر برو به آدرس: **http://localhost:3000**
- برای **خاموش‌کردن** سایت، در ترمینال `Ctrl + C` بزن.

> ✅ **ترتیب درست برای اولین بار:**
> ```bash
> npm install
> cp .env.example .env      # و بعد .env را باز کن و رمزها را پر کن
> npm run db:migrate
> npm run db:seed
> npm run dev
> ```

---

<a name="deploy"></a>
## 🚀 ۳) گذاشتن روی سرور (دیپلوی)

«دیپلوی» یعنی سایت را جایی بگذاری که **روی اینترنت** و برای همه قابل دسترس باشد (نه فقط روی لپ‌تاپ خودت).

### 🧱 اول یک تغییر مهم: عوض‌کردن دیتابیس به Postgres
چون SQLite (فایل) روی اکثر سرورهای ابری کار نمی‌کند، باید دیتابیس را به **PostgreSQL** عوض کنی. این کار فقط **۲ تغییر کوچک** دارد:

1. در فایل `prisma/schema.prisma` این خط:
   ```prisma
   provider = "sqlite"
   ```
   را به این تغییر بده:
   ```prisma
   provider = "postgresql"
   ```
2. در تنظیمات سرور، مقدار `DATABASE_URL` را به آدرس دیتابیس Postgresِ ابری‌ات بده (سرویس‌های زیر این آدرس را بهت می‌دهند).

---

### 🏆 کدام سرور را انتخاب کنم؟ (مقایسه)

| گزینه | سختی | مناسب چه کسی؟ | نکته |
|---|---|---|---|
| 🥇 **Liara** (ایرانی) | 🟢 آسان | تو (شروع‌کننده، داخل ایران) | پرداخت ریالی، بدون نیاز به VPN، هم اپ و هم دیتابیس Postgres دارد |
| 🥈 **Vercel + Neon** | 🟡 متوسط | کسی که به اینترنت آزاد دسترسی دارد | ممکن است از ایران تحریم/مسدود باشد |
| 🥉 **سرور مجازی (VPS)** | 🔴 سخت | کاربر حرفه‌ای | کنترل کامل ولی باید همه‌چیز را دستی تنظیم کنی |

> 🎯 **توصیه‌ی من برای تو:** **Liara**. چون ایرانی است، پرداخت ریالی دارد، نیاز به VPN ندارد، و هم میزبانی Next.js و هم دیتابیس Postgres را با هم می‌دهد.

---

### 📋 مراحل دیپلوی روی Liara (ساده)

> 📌 این مراحل کلی است؛ دکمه‌های دقیق ممکن است در پنل Liara کمی فرق کند، ولی مسیر همین است. مستندات: `docs.liara.ir`

```bash
# ۱) نصب ابزار Liara روی کامپیوتر
npm install -g @liara/cli

# ۲) ورود به حساب
liara login
```

۳) از **پنل Liara** یک **دیتابیس PostgreSQL** بساز. بعد از ساخت، یک **آدرس اتصال (DATABASE_URL)** بهت می‌دهد — آن را کپی کن.

۴) در `prisma/schema.prisma` مقدار `provider` را به `postgresql` تغییر بده (همان بالا گفتم).

۵) از پنل Liara یک **اپلیکیشن از نوع Next.js** بساز و این **سه متغیر محیطی** را در تنظیماتش وارد کن:

| متغیر | مقدار |
|---|---|
| `DATABASE_URL` | همان آدرسی که دیتابیس Liara داد |
| `ADMIN_PASSWORD` | رمز قوی برای پنل |
| `SESSION_SECRET` | خروجی `openssl rand -base64 32` |

```bash
# ۶) فرستادن پروژه روی سرور
liara deploy
```

۷) **یک‌بار** جدول‌ها و داده‌ی اولیه را روی دیتابیس ابری بساز. ساده‌ترین راه: روی کامپیوتر خودت `DATABASE_URL` را موقتاً برابر آدرس دیتابیس ابری بگذار و این دو را بزن:
```bash
npx prisma migrate deploy
npm run db:seed
```

🎉 تمام! سایت روی آدرسی که Liara می‌دهد بالا می‌آید و پنل مدیریت روی `آدرس‌سایت/admin/login` در دسترس است.

---

<a name="checklist"></a>
## ✅ ۴) چک‌لیست نهایی

| مرحله | روی لپ‌تاپ خودم (تست) | روی سرور (واقعی) |
|---|---|---|
| دیتابیس | 📄 SQLite (فایل) — کاری نکن | 🐘 Postgres — provider را عوض کن |
| نصب کتابخانه‌ها | `npm install` | معمولاً سرور خودش انجام می‌دهد |
| تنظیمات `.env` | فایل `.env` بساز و پر کن | متغیرها را در پنل سرور وارد کن |
| ساخت جدول‌ها | `npm run db:migrate` | `npx prisma migrate deploy` |
| داده‌ی اولیه | `npm run db:seed` | `npm run db:seed` (یک‌بار) |
| اجرا | `npm run dev` | سرور خودش با `build` + `start` اجرا می‌کند |

> 💡 **نکته‌ی مهم:** هنوز **Node.js** روی این محیط نصب نیست، پس این دستورها را باید روی **کامپیوتر خودت** اجرا کنی. اگر Node نصب نداری، اول از سایت `nodejs.org` نسخه‌ی LTS را نصب کن.

> 🙋 اگر هر کدام از این مراحل گیر کردی، بگو دقیقاً کدام قدم و چه پیغام خطایی دیدی، تا همان را با هم حل کنیم.

</div>
